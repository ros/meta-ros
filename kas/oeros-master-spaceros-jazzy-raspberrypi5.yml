header:
  version: 14
  includes:
    - kas/yocto/master.yml
    - kas/spaceros/jazzy-2026.07.yml
    - kas/machine/raspberrypi5.yml
    - kas/common.yml
    - kas/layer/qt5.yml

repos:
  realsense:
    branch: "wrynose"
  spaceros:
    branch: "master-next"

local_conf_header:
  spaceros:
    # Temporary workaround to fix ffmpeg build failure
    # Unknown option "--enable-libshaderc"
    EXTRA_OECONF:remove:pn-ffmpeg = "--enable-libshaderc"
